# Core dependencies (mirrors pyproject.toml — pip install iwfm-io)
numpy>=1.20
pandas>=1.2
h5py>=3.0
matplotlib>=3.3

# Optional geometry output (pip install iwfm-io[geo])
# geopandas>=0.10
# shapely>=1.8
